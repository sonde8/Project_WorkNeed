<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Workneed.workneed.Attendance.mapper.TeamAttendMapper">

    <select id="findTeamUserId" resultType="long">
        SELECT dept_id
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="findTeamMembers" resultType="com.Workneed.workneed.Attendance.dto.TeamMemberDTO">
        SELECT
            u.user_id AS userId,
            u.user_name AS userName,
            r.rank_name AS rankName
        FROM user u
                 JOIN ranks r ON u.rank_id = r.rank_id
        WHERE u.dept_id = #{deptId}
          AND u.user_status = 'ACTIVE'
        ORDER BY r.rank_level DESC, u.user_name ASC
    </select>
</mapper>