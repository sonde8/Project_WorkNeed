<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Workneed.workneed.Schedule.mapper.ScheduleParticipantMapper">

    <insert id="inviteOwner">
        INSERT INTO schedule_participant (schedule_id, user_id, role)
        VALUES
        (#{scheduleId}, #{userId}, 'OWNER')
    </insert>

    <insert id="inviteTeam">
        INSERT INTO schedule_participant (schedule_id, user_id, role)
        VALUES
        <foreach collection="userIds" item="uid" separator=",">
            (#{scheduleId}, #{uid}, 'MEMBER')
        </foreach>
    </insert>

    <insert id="inviteCompany">
        INSERT INTO schedule_participant (schedule_id, user_id, role)
        SELECT
        #{scheduleId},
        u.user_id,
        'MEMBER'
        FROM user u
        WHERE u.user_status = 'ACTIVE'
        AND u.user_id != #{ownerId}
    </insert>
</mapper>
