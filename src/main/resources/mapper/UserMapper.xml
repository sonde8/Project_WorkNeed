<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Workneed.workneed.Members.mapper.UserMapper">

    <select id="findAll" resultType="com.Workneed.workneed.Members.entity.User">
        SELECT
        user_id,
        user_login_id,
        user_email,
        user_password,
        user_name,
        user_profile_image,
        user_status,
        user_birthday,
        rank_id,
        dept_id,
        user_created_at,
        user_updated_at
        FROM User
    </select>

    <select id="findByLoginId"
            parameterType="string"
            resultType="com.Workneed.workneed.Members.entity.User">
        SELECT
        user_id,
        user_login_id,
        user_email,
        user_password,
        user_name,
        user_profile_image,
        user_status,
        user_birthday,
        rank_id,
        dept_id,
        user_created_at,
        user_updated_at
        FROM User
        WHERE user_login_id = #{loginId}
    </select>

    <select id="findByEmail"
            parameterType="string"
            resultType="com.Workneed.workneed.Members.entity.User">
        SELECT
        user_id,
        user_login_id,
        user_email,
        user_password,
        user_name,
        user_profile_image,
        user_status,
        user_birthday,
        rank_id,
        dept_id,
        user_created_at,
        user_updated_at
        FROM User
        WHERE user_email = #{email}
    </select>

    <insert id="insertUser" parameterType="com.Workneed.workneed.Members.entity.User">
        INSERT INTO User (
        user_login_id,
        user_email,
        user_password,
        user_name,
        user_profile_image,
        user_status,
        user_birthday,
        rank_id,
        dept_id
        ) VALUES (
        #{userLoginId},
        #{userEmail},
        #{userPassword},
        #{userName},
        #{userProfileImage},
        #{userStatus},
        #{userBirthday},
        #{rankId},
        #{deptId}
        )
    </insert>

    <!--아이디찾기이메일-->
    <select id="findByNameAndEmail"
            resultType="com.Workneed.workneed.Members.entity.User">
        SELECT
        user_id,
        user_login_id,
        user_email,
        user_name
        FROM User
        WHERE user_name = #{name}
        AND user_email = #{email}
    </select>





    <update id="updatePassword">
        UPDATE User
        SET user_password = #{userPassword}
        WHERE user_id = #{userId}
    </update>

    <update id="updateUser" parameterType="com.Workneed.workneed.Members.entity.User">
        UPDATE User
        SET
        user_email = #{userEmail},
        user_password = #{userPassword},
        user_name = #{userName},
        user_profile_image = #{userProfileImage},
        user_status = #{userStatus},
        rank_id = #{rankId},
        dept_id = #{deptId}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="long">
        DELETE FROM User
        WHERE user_id = #{userId}
    </delete>

</mapper>
