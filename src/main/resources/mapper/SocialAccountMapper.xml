<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Workneed.workneed.Members.mapper.SocialAccountMapper">

    <select id="findByProviderAndProviderUserId"
            resultType="com.Workneed.workneed.Members.entity.SocialAccount">
        SELECT *
        FROM SocialAccount
        WHERE social_provider = #{provider}
        AND social_provider_user_id = #{providerUserId}
    </select>

    <select id="findByUserIdAndProvider"
            resultType="com.Workneed.workneed.Members.entity.SocialAccount">
        SELECT *
        FROM SocialAccount
        WHERE user_id = #{userId}
        AND social_provider = #{provider}
    </select>

    <insert id="insertSocialAccount">
        INSERT INTO SocialAccount (
        user_id,
        social_provider,
        social_provider_user_id,
        social_email,
        social_access_token,
        social_refresh_token
        ) VALUES (
        #{userId},
        #{socialProvider},
        #{socialProviderUserId},
        #{socialEmail},
        #{socialAccessToken},
        #{socialRefreshToken}
        )
    </insert>

    <delete id="deleteByUserIdAndProvider">
        DELETE FROM SocialAccount
        WHERE user_id = #{userId}
        AND social_provider = #{provider}
    </delete>

</mapper>
